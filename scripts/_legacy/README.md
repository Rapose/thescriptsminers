# Legacy scripts

This folder contains the previous flow-based scripts preserved for compatibility.

- Legacy wrappers now reference files inside `scripts/_legacy/...`.
- New development should target the modular scripts under `scripts/*` (outside `_legacy`).

Use this folder only when you explicitly need historical behavior or backward compatibility.
